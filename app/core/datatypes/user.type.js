export class User {
  // number = 0;;
  // token:string;
  // // picture:string;
  // pictureSmall:string;
  // username: string;
  // displayName: string;
  // bandId:number;
  // bandName:string;
  // deviceId:string;
  // bands: [];
  // fb:boolean;
  // facebookToken:string;
  // spotifyToken:string;
  // profile: {about:string, instrument:string, experience:string, styles:[], influencies:[]};
  // contact: {bio:string, city:string, area:string, zip:string};
  // channels: [{channel:string, name:string, url:string}];

  constructor(  id,token) {
      this.id = id;
      this.token = token;
      
    }
}
